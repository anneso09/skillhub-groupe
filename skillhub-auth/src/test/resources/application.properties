# Config pour les tests uniquement - utilise H2 en memoire
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Hibernate cree les tables automatiquement pour les tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=false

# Master Key fictive pour les tests uniquement
# Ne jamais utiliser cette cle en production !
app.master.key=test_master_key_for_ci_only_32chars!